Create new function component named Error

We will expect that this component will receive the error message STATE value as a PROP.

We will destructure errMsg from the PROPS Object in the parameter list.
